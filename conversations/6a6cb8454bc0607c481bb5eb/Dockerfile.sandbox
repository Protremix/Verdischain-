FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn httpx structlog pydantic asyncpg

COPY plugin_sandbox.py sandbox_api.py pg_persistence.py /app/

EXPOSE 4200

CMD ["uvicorn", "sandbox_api:app", "--host", "0.0.0.0", "--port", "4200", "--workers", "1"]
