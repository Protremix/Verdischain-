FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn httpx structlog pydantic

COPY core_agents.py core_agents_api.py /app/

EXPOSE 3900

CMD ["uvicorn", "core_agents_api:app", "--host", "0.0.0.0", "--port", "3900", "--workers", "1"]
