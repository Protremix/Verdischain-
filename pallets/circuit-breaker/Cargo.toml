[package]
name = "pallet-circuit-breaker"
version = "0.1.0"
description = "Governance-controlled circuit breaker for emergency pallet pausing"
license = "MIT"
edition = "2021"
publish = false

[dependencies]
codec = { workspace = true, features = ["derive", "max-encoded-len"] }
scale-info = { workspace = true, features = ["derive"] }
frame-support = { workspace = true }
frame-system = { workspace = true }
frame-benchmarking = { workspace = true, optional = true }
sp-std = { workspace = true }

[features]
default = ["std"]
std = ["codec/std", "scale-info/std", "frame-support/std", "frame-system/std",
    "frame-benchmarking?/std", "sp-std/std"]
runtime-benchmarks = [
    "frame-benchmarking/runtime-benchmarks","frame-support/runtime-benchmarks", "frame-system/runtime-benchmarks"]
try-runtime = ["frame-support/try-runtime"]

[dev-dependencies]
sp-io = { workspace = true }
sp-core = { workspace = true }
sp-runtime = { workspace = true, features = ["std"] }
frame-support = { workspace = true, features = ["std"] }
frame-system = { workspace = true, features = ["std"] }
sp-keyring = { workspace = true }
